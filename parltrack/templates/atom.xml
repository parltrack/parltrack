<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
	<title>Parltrack watcher Feed - {{path}}</title>
	<link rel="alternate" type="text/html" href="{{path}}"/>
	<link rel="self" type="application/atom+xml" href="{{path}}?format=atom"/>
	<updated>{% if dossiers.0.updated %}{{dossiers.0.updated}}{%else%}{{dossiers.0.created}}{%endif%}</updated>
   <generator uri="http://github.org/pudo/parltrack">parltrack</generator>
   <id>{{request.url}}</id>
   {% for d in dossiers %}
	<entry>
		<title>{{d.procedure.reference}} {{d.procedure.title}}</title>
		<link rel="alternate" type="text/html" href="{{request.url_root}}dossier/{{d.procedure.reference}}"/>
      {% if d.updated %}<updated>{{d.updated}}</updated>{%else%}<created>{{d.created}}</created>{%endif%}
      <id>{{d.meta.source}}</id>
		<content type="html" xml:lang="en"><![CDATA[
         {% if d.activities and d.activities[-1].documents and d.activities[-1].documents.0.url %}
           <a href="{{d.activities[-1].documents.0.url}}">{{d.activities[-1].documents.0.title}}</a><br />
         {%endif%}
           Stage Reached: {{d.procedure['stage_reached']}}
         {% if d.changes %}
           <ul>
           {%for c in d.changes.values()[-1]%}
             <li><dl><dt>
               {%if c.type == 'added' %}Added: {%endif%}
               {%if c.type == 'deleted' %}Deleted: {%endif%}
               {%if c.type =='changed' %}Changed: {%endif%}
               {% for f in c.path%}{{f}}/{%endfor%}</dt>
               <dd>
                 {%if c.type == 'changed' %}
                   new value: {{c.data.0|safe}}<br />
                   old value: {{c.data.1|safe}}
                 {%elif c.type == 'added'%}
                   {{c.data|safe}}
                 {%elif c.type == 'deleted'%}
                   {{c.data|safe}}
                 {%endif%}
               </dd></dl>
             </li>
           {%endfor%}
           </ul>
         {%endif%}
]]></content>
	</entry>
   {%endfor%}
</feed>
